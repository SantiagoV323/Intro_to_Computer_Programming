# Quiz 1

1. Write a function header for `main()`.
	```python
	def main():
	```
2. Write an input statement that prompts the user to enter an integer for the variable `miles`.
	```python
	miles = int(input())
	```
3. Assume you have two variables: `miles` and `gallons`. Write a statement to calculate miles per gallon and store the result in `mpg`.
	```python
	mpg = miles / gallons
	```
4. Write a function call to `main()`.
	```python
	main()
	```